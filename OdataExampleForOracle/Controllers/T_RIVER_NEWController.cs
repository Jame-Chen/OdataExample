//------------------------------------------------------------------------------
// <auto-generated>
//    此代码是根据模板生成的。
//
//    手动更改此文件可能会导致应用程序中发生异常行为。
//    如果重新生成代码，则将覆盖对此文件的手动更改。
// </auto-generated>
//------------------------------------------------------------------------------

namespace OdataExampleForOracle.Controllers
{
    using System.Data.Entity;
    using System.Data.Entity.Infrastructure;
    using System.Linq;
    using System.Net;
    using System.Net.Http;
    using System.Web.Http;
    using System.Web.Http.ModelBinding;
    using System.Web.Http.OData;
    using System.Web.Http.OData.Routing;
    using OdataExampleForOracle.Models;
    public partial class T_RIVER_NEWController:ODataController
    {
            private SJZXEntities db = new SJZXEntities();
    
            // GET: odata/T_RIVER_NEW
            [EnableQuery]
            public IQueryable<T_RIVER_NEW> GetT_RIVER_NEW()
            {
                return db.T_RIVER_NEW;
            }
    
            // GET: odata/T_RIVER_NEW(5)
            [EnableQuery]
            public SingleResult<T_RIVER_NEW> GetT_RIVER_NEW([FromODataUri] decimal key)
            {
                return SingleResult.Create(db.T_RIVER_NEW.Where(w => w.OBJECTID == key));
            }
    
            // PUT: odata/T_RIVER_NEW(5)
            public IHttpActionResult Put([FromODataUri] decimal key, Delta<T_RIVER_NEW> patch)
            {
                Validate(patch.GetEntity());
    
                if (!ModelState.IsValid)
                {
                    return BadRequest(ModelState);
                }
    
                T_RIVER_NEW T_RIVER_NEW = db.T_RIVER_NEW.Find(key);
                if (T_RIVER_NEW == null)
                {
                    return NotFound();
                }
    
                patch.Put(T_RIVER_NEW);
    
                try
                {
                    db.SaveChanges();
                }
                catch (DbUpdateConcurrencyException)
                {
                    if (!T_RIVER_NEWExists(key))
                    {
                        return NotFound();
                    }
                    else
                    {
                        throw;
                    }
                }
    
                return Updated(T_RIVER_NEW);
            }
    
            // POST: odata/T_RIVER_NEW
            public IHttpActionResult Post(T_RIVER_NEW T_RIVER_NEW)
            {
                if (!ModelState.IsValid)
                {
                    return BadRequest(ModelState);
                }
    
                db.T_RIVER_NEW.Add(T_RIVER_NEW);
                db.SaveChanges();
    
                return Created(T_RIVER_NEW);
            }
    
            // PATCH: odata/T_RIVER_NEW(5)
            [AcceptVerbs("PATCH", "MERGE")]
            public IHttpActionResult Patch([FromODataUri] decimal key, Delta<T_RIVER_NEW> patch)
            {
                Validate(patch.GetEntity());
    
                if (!ModelState.IsValid)
                {
                    return BadRequest(ModelState);
                }
    
                T_RIVER_NEW T_RIVER_NEW = db.T_RIVER_NEW.Find(key);
                if (T_RIVER_NEW == null)
                {
                    return NotFound();
                }
    
                patch.Patch(T_RIVER_NEW);
    
                try
                {
                    db.SaveChanges();
                }
                catch (DbUpdateConcurrencyException)
                {
                    if (!T_RIVER_NEWExists(key))
                    {
                        return NotFound();
                    }
                    else
                    {
                        throw;
                    }
                }
    
                return Updated(T_RIVER_NEW);
            }
    
            // DELETE: odata/T_RIVER_NEW(5)
            public IHttpActionResult Delete([FromODataUri] decimal key)
            {
                T_RIVER_NEW T_RIVER_NEW = db.T_RIVER_NEW.Find(key);
                if (T_RIVER_NEW == null)
                {
                    return NotFound();
                }
    
                db.T_RIVER_NEW.Remove(T_RIVER_NEW);
                db.SaveChanges();
    
                return StatusCode(HttpStatusCode.NoContent);
            }
    
            protected override void Dispose(bool disposing)
            {
                if (disposing)
                {
                    db.Dispose();
                }
                base.Dispose(disposing);
            }
    
            private bool T_RIVER_NEWExists(decimal key)
            {
                return db.T_RIVER_NEW.Count(e => e.OBJECTID == key) > 0;
            }
      
    }
}
