//------------------------------------------------------------------------------
// <auto-generated>
//    此代码是根据模板生成的。
//
//    手动更改此文件可能会导致应用程序中发生异常行为。
//    如果重新生成代码，则将覆盖对此文件的手动更改。
// </auto-generated>
//------------------------------------------------------------------------------

namespace OdataExampleForOracle.Controllers
{
    using System.Data.Entity;
    using System.Data.Entity.Infrastructure;
    using System.Linq;
    using System.Net;
    using System.Net.Http;
    using System.Web.Http;
    using System.Web.Http.ModelBinding;
    using System.Web.Http.OData;
    using System.Web.Http.OData.Routing;
    using OdataExampleForOracle.Models;
    public partial class T_DRAINPUMPController:ODataController
    {
            private SJZXEntities db = new SJZXEntities();
    
            // GET: odata/T_DRAINPUMP
            [EnableQuery]
            public IQueryable<T_DRAINPUMP> GetT_DRAINPUMP()
            {
                return db.T_DRAINPUMP;
            }
    
            // GET: odata/T_DRAINPUMP(5)
            [EnableQuery]
            public SingleResult<T_DRAINPUMP> GetT_DRAINPUMP([FromODataUri] decimal key)
            {
                return SingleResult.Create(db.T_DRAINPUMP.Where(w => w.OBJECTID == key));
            }
    
            // PUT: odata/T_DRAINPUMP(5)
            public IHttpActionResult Put([FromODataUri] decimal key, Delta<T_DRAINPUMP> patch)
            {
                Validate(patch.GetEntity());
    
                if (!ModelState.IsValid)
                {
                    return BadRequest(ModelState);
                }
    
                T_DRAINPUMP T_DRAINPUMP = db.T_DRAINPUMP.Find(key);
                if (T_DRAINPUMP == null)
                {
                    return NotFound();
                }
    
                patch.Put(T_DRAINPUMP);
    
                try
                {
                    db.SaveChanges();
                }
                catch (DbUpdateConcurrencyException)
                {
                    if (!T_DRAINPUMPExists(key))
                    {
                        return NotFound();
                    }
                    else
                    {
                        throw;
                    }
                }
    
                return Updated(T_DRAINPUMP);
            }
    
            // POST: odata/T_DRAINPUMP
            public IHttpActionResult Post(T_DRAINPUMP T_DRAINPUMP)
            {
                if (!ModelState.IsValid)
                {
                    return BadRequest(ModelState);
                }
    
                db.T_DRAINPUMP.Add(T_DRAINPUMP);
                db.SaveChanges();
    
                return Created(T_DRAINPUMP);
            }
    
            // PATCH: odata/T_DRAINPUMP(5)
            [AcceptVerbs("PATCH", "MERGE")]
            public IHttpActionResult Patch([FromODataUri] decimal key, Delta<T_DRAINPUMP> patch)
            {
                Validate(patch.GetEntity());
    
                if (!ModelState.IsValid)
                {
                    return BadRequest(ModelState);
                }
    
                T_DRAINPUMP T_DRAINPUMP = db.T_DRAINPUMP.Find(key);
                if (T_DRAINPUMP == null)
                {
                    return NotFound();
                }
    
                patch.Patch(T_DRAINPUMP);
    
                try
                {
                    db.SaveChanges();
                }
                catch (DbUpdateConcurrencyException)
                {
                    if (!T_DRAINPUMPExists(key))
                    {
                        return NotFound();
                    }
                    else
                    {
                        throw;
                    }
                }
    
                return Updated(T_DRAINPUMP);
            }
    
            // DELETE: odata/T_DRAINPUMP(5)
            public IHttpActionResult Delete([FromODataUri] decimal key)
            {
                T_DRAINPUMP T_DRAINPUMP = db.T_DRAINPUMP.Find(key);
                if (T_DRAINPUMP == null)
                {
                    return NotFound();
                }
    
                db.T_DRAINPUMP.Remove(T_DRAINPUMP);
                db.SaveChanges();
    
                return StatusCode(HttpStatusCode.NoContent);
            }
    
            protected override void Dispose(bool disposing)
            {
                if (disposing)
                {
                    db.Dispose();
                }
                base.Dispose(disposing);
            }
    
            private bool T_DRAINPUMPExists(decimal key)
            {
                return db.T_DRAINPUMP.Count(e => e.OBJECTID == key) > 0;
            }
      
    }
}
