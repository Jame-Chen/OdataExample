//------------------------------------------------------------------------------
// <auto-generated>
//    此代码是根据模板生成的。
//
//    手动更改此文件可能会导致应用程序中发生异常行为。
//    如果重新生成代码，则将覆盖对此文件的手动更改。
// </auto-generated>
//------------------------------------------------------------------------------

namespace OdataExampleForOracle.Controllers
{
    using System.Data.Entity;
    using System.Data.Entity.Infrastructure;
    using System.Linq;
    using System.Net;
    using System.Net.Http;
    using System.Web.Http;
    using System.Web.Http.ModelBinding;
    using System.Web.Http.OData;
    using System.Web.Http.OData.Routing;
    using OdataExampleForOracle.Models;
    public partial class V_APP_RIVERController:ODataController
    {
            private SJZXEntities db = new SJZXEntities();
    
            // GET: odata/V_APP_RIVER
            [EnableQuery]
            public IQueryable<V_APP_RIVER> GetV_APP_RIVER()
            {
                return db.V_APP_RIVER;
            }
    
            // GET: odata/V_APP_RIVER(5)
            [EnableQuery]
            public SingleResult<V_APP_RIVER> GetV_APP_RIVER([FromODataUri] decimal key)
            {
                return SingleResult.Create(db.V_APP_RIVER.Where(w => w.OBJECTID == key));
            }
    
            // PUT: odata/V_APP_RIVER(5)
            public IHttpActionResult Put([FromODataUri] decimal key, Delta<V_APP_RIVER> patch)
            {
                Validate(patch.GetEntity());
    
                if (!ModelState.IsValid)
                {
                    return BadRequest(ModelState);
                }
    
                V_APP_RIVER V_APP_RIVER = db.V_APP_RIVER.Find(key);
                if (V_APP_RIVER == null)
                {
                    return NotFound();
                }
    
                patch.Put(V_APP_RIVER);
    
                try
                {
                    db.SaveChanges();
                }
                catch (DbUpdateConcurrencyException)
                {
                    if (!V_APP_RIVERExists(key))
                    {
                        return NotFound();
                    }
                    else
                    {
                        throw;
                    }
                }
    
                return Updated(V_APP_RIVER);
            }
    
            // POST: odata/V_APP_RIVER
            public IHttpActionResult Post(V_APP_RIVER V_APP_RIVER)
            {
                if (!ModelState.IsValid)
                {
                    return BadRequest(ModelState);
                }
    
                db.V_APP_RIVER.Add(V_APP_RIVER);
                db.SaveChanges();
    
                return Created(V_APP_RIVER);
            }
    
            // PATCH: odata/V_APP_RIVER(5)
            [AcceptVerbs("PATCH", "MERGE")]
            public IHttpActionResult Patch([FromODataUri] decimal key, Delta<V_APP_RIVER> patch)
            {
                Validate(patch.GetEntity());
    
                if (!ModelState.IsValid)
                {
                    return BadRequest(ModelState);
                }
    
                V_APP_RIVER V_APP_RIVER = db.V_APP_RIVER.Find(key);
                if (V_APP_RIVER == null)
                {
                    return NotFound();
                }
    
                patch.Patch(V_APP_RIVER);
    
                try
                {
                    db.SaveChanges();
                }
                catch (DbUpdateConcurrencyException)
                {
                    if (!V_APP_RIVERExists(key))
                    {
                        return NotFound();
                    }
                    else
                    {
                        throw;
                    }
                }
    
                return Updated(V_APP_RIVER);
            }
    
            // DELETE: odata/V_APP_RIVER(5)
            public IHttpActionResult Delete([FromODataUri] decimal key)
            {
                V_APP_RIVER V_APP_RIVER = db.V_APP_RIVER.Find(key);
                if (V_APP_RIVER == null)
                {
                    return NotFound();
                }
    
                db.V_APP_RIVER.Remove(V_APP_RIVER);
                db.SaveChanges();
    
                return StatusCode(HttpStatusCode.NoContent);
            }
    
            protected override void Dispose(bool disposing)
            {
                if (disposing)
                {
                    db.Dispose();
                }
                base.Dispose(disposing);
            }
    
            private bool V_APP_RIVERExists(decimal key)
            {
                return db.V_APP_RIVER.Count(e => e.OBJECTID == key) > 0;
            }
      
    }
}
